apiVersion: v1
kind: Service
metadata:
  name: fishchain-backend
  namespace: fishchain
spec:
  selector:
    app: fishchain-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
